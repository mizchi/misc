import type { } from "npm:@types/react"

import $ from "jsr:@david/dax@0.40.1";

Deno.test("build", async () => {
  // $.cd("__fixtures__");
  // await $`ENTRY=01.ts deno run -A npm:vite build`;
  // await $`ENTRY=02.ts deno run -A npm:vite build`;
  // await $`ENTRY=03.ts deno run -A npm:vite build`;
  // await $`ENTRY=04.ts deno run -A npm:vite build`;
  // await $`ENTRY=05.tsx deno run -A npm:vite build`;
});
